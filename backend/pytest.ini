[pytest]
# Put the backend/ directory on sys.path so tests can `import app`.
#
# Without this, `python -m pytest` works (the -m form adds the current directory
# to sys.path) but the `pytest` console script does not, which is a confusing
# difference to hit. Declaring it here makes both forms behave the same.
pythonpath = .

testpaths = tests
